# TAF 全量代码审计报告

**日期**: 2026-08-04  
**范围**: `/root/TAF/` 全部 Python + 前端代码，含数据库  
**规模**: ~5,147 行（Python 4,000+ + HTML 1,000+），21 个 .py 文件，1 个 .html 文件  
**方法**: 单代理 6 维度系统审计 + grep 模式扫描 + DB 结构验证

---

## 综合评分

| 维度 | 得分 | 权重 | 加权 |
|------|:----:|:----:|:----:|
| 安全性 | 6/10 | 20% | 1.20 |
| 代码质量 | 7/10 | 20% | 1.40 |
| 架构设计 | 8/10 | 20% | 1.60 |
| 性能 | 7/10 | 15% | 1.05 |
| 业务逻辑 | 8/10 | 15% | 1.20 |
| 可运维性 | 6/10 | 10% | 0.60 |
| **综合** | | | **7.05/10** |

与上次审计（5.6/10，安全性 3.5 拖分）相比提升显著：P0 全部修复，模块化拆分完成，认证骨架就位。主要扣分仍在安全性和可运维性。

---

## P0 — 严重

| # | 位置 | 问题 | 影响 | 修复建议 |
|---|------|------|------|---------|
| **P0-1** | `config.py:48` | `database_url_sync` 不对密码做 `quote_plus()`，而 `database_url` (line 43) 做了 | 密码含 `@/#/%` 时 sync URL 断裂（当前未使用 sync 引擎，危害有限） | 统一步骤：`pw = quote_plus(self.db_password)` |

---

## P1 — 高危

| # | 位置 | 问题 | 影响 | 修复建议 |
|---|------|------|------|---------|
| **P1-1** | `evaluation.py:76,104` | `score_history` 和 `standard_change_log` 两张表**不存在**于数据库中。评估和标准切换操作尝试写入这两张表均失败，被 `except Exception` 静默吞掉 | 评分历史完全丢失，标准切换日志不可追溯。每次评估都是「一次性」的 | 在 `models/database.py` 添加 `ScoreHistory` 和 `StandardChangeLog` 模型，或建 SQL 表 |
| **P1-2** | `proposal.py:335` `visualization.py:259,371` `versioning.py:141,170` | 5 处 `except Exception` 裸捕获 — 旧 `deliverables.py` 的 P2-5 修复未迁移到拆分后的模块 | 坚果云上传失败、KDE 失败、DeepSeek 降级等场景吞掉所有异常信息，排障困难 | 统一改为 `logger.exception("描述")` |
| **P1-3** | `deps.py:45-57` | `get_current_user()` 认证函数已定义但**未在任何路由上强制执行** — 所有端点公开可访问 | 生产环境无认证保护，任何人可调用 POST/PUT/DELETE | 在路由上添加 `Depends(get_current_user)`，或至少对写操作端点加保护 |

---

## P2 — 中危

| # | 位置 | 问题 | 影响 | 修复建议 |
|---|------|------|------|---------|
| **P2-1** | `proposal.py:11` | 重复导入 `from fastapi import HTTPException`（line 8 已导入） | 无功能影响，代码异味 | 删除 line 11 |
| **P2-2** | `proposal.py:198,234,240,250,280,285,307,338` | 异步端点中大量同步 I/O：`open()`、`subprocess.run()`、`shutil.rmtree()` | 阻塞事件循环，并发请求时串行化。坚果云上传使用 curl 子进程属设计妥协（避免 Python requests HTTP/2 兼容问题） | 长期方案：`asyncio.to_thread()` 包装；短期：当前影响可接受，TP99 延迟取决于坚果云响应 |
| **P2-3** | `visualization.py` top-level: `import json`, `import matplotlib.pyplot`, `import numpy` | 顶层未使用，实际使用在函数内部 inline import | 增加模块加载时间 | 移除顶层未用 imports |
| **P2-4** | `frontend/index.html` | `boundStandard.config.total_items` 和 `prerequisite_count` 字段在 API 返回的 raw config 中不存在，前端取值永远为 0 | 标准详情页显示 "0 项指标 · 0 必选"，误导用户 | 后端在返回标准时计算 `total_items` + `prerequisite_count`，或前端从 `config.items.length` 提取 |
| **P2-5** | `evaluation.py:76,104,211` | 3 处 `except Exception` + `logger.debug` 静默处理 | 评分历史/日志失败无感知，但属非关键路径 | 改为 `logger.warning` 至少保留一条可见日志 |
| **P2-6** | `config.py:54-58` | `get_settings()` 在 validator 之后二次检查空密码 + warning — 但 validator 已 `raise ValueError` 拒绝空密码，此二次检查**永远不会触发** | 死代码，维护困惑 | 删除 `if not s.db_password:` 检查块 |

---

## P3 — 低危 / 改进建议

| # | 位置 | 问题 | 建议 |
|---|------|------|------|
| P3-1 | `main.py:70-71` | `__import__("sqlalchemy").text("SELECT 1")` 动态导入 | 改为正常 top-level import |
| P3-2 | `proposal.py:163` | `import markdown` 和 `weasyprint` 在函数内部 inline import，与 top-level 的 `import json` 风格不一致 | 统一策略：非核心依赖可保持 inline，核心依赖 top-level |
| P3-3 | `basemaps.py:83` | 同步 `os.path.exists()` 在异步函数中 | 轻量操作，影响可忽略 |
| P3-4 | 全局 | 无 API 限流 | 建议对 POST 端点加 slowapi 限流 |
| P3-5 | 全局 | 无结构化日志格式 | 建议 JSON 格式日志便于检索 |

---

## 正向亮点

- **模块化拆分**：`deliverables.py` (1889行) → 7 模块包，职责清晰，交叉引用明确
- **评估引擎设计**：`EvaluationEngine` 类解耦干净，`is_optional_facility` 跳过逻辑正确
- **评估引擎**：`_best_facility` 处理同项多设施取最优值，`confirmed=installed=满分` 符合业务预期
- **安全性改善**：CORS 配置化（非 `*`），API Token 认证骨架就位，文件上传有扩展名白名单+路径遍历防护
- **种子数据**：`seed_data.sql` 4 表全量 pg_dump，可一键重建 DB
- **数据库连接**：`get_engine()` 工厂模式 + `async_sessionmaker` 延迟创建，通过 P2 审计
- **无硬编码密码**：所有凭据从环境变量读取，`.env` 在 `.gitignore` 中
- **前端**：`PRODUCT_LINES` 提取为全局常量，`deleteFacility` 已走 `API.base` 代理
- **CSRF/注入**：SQLAlchemy ORM + 参数化查询，无 SQL 注入风险；FastAPI + Pydantic 自动校验

---

## 优先级修复方案

### 立即 (P0)
1. `config.py:48` — `database_url_sync` 加 `quote_plus` (1 行)

### 本周 (P1)
2. 补充 `score_history` + `standard_change_log` 两张表
3. 5 处 `except Exception` → `logger.exception`
4. 关键写操作端点加 `Depends(get_current_user)`

### 下个迭代 (P2)
5. 删除重复 import，清理未用 imports
6. 前端 `boundStandard.config` 显示修复
7. 死代码清理（config.py 二次检查）
